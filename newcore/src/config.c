#include "../include/config.h"
#include <stdio.h>

void config_load(const char* filename) {
    // @pcs: Stub implementation for config loading
    printf("[CONFIG] Loading config from %s (stub)\n", filename);
    // TODO: Parse config file and populate NcConfig
} 