#ifndef NC_SAVESTATE_H
#define NC_SAVESTATE_H

// @pcs: Load a savestate (stub)
int savestate_load(const char* filename);

// @pcs: Save a savestate (stub)
int savestate_save(const char* filename);

#endif // NC_SAVESTATE_H 